# dsh-skill-manager bundle patch — inserts its row into the web plugin roster.
# Install with:  dsh plugin --profile web add <path-or-npm-name>
- insert:
    - id: ui-skill-manager
      name: dsh-skill-manager
