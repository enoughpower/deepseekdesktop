# Desktop-app overlay: register the session cost meter (dsh-cost-meter).
# NOTE: the name must be quoted — linkBundledPlugins only collects quoted
# name: fields from - insert: blocks.
- insert:
    - id: cost-meter
      name: 'dsh-cost-meter'
